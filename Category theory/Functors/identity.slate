%../../../../logics/hlm%

$identity(
  u: %Element(
    $../../Essentials/Numbers/Natural/"Natural numbers",
    auto = %true
  ),
  𝓒: %Element(
    $../Categories(u = {u}),
    auto = %true
  )
): %ExplicitOperator {
  display = [$SubSup(
    body = 'id',
    sub = 𝓒
  )],
  definition = [$Functors(
      u = {u},
      𝓒 = {𝓒},
      𝓓 = {𝓒}
    ).functor(
      𝐗 = {
        #(𝐗: %Element($../Objects(
          u = {u},
          𝓒 = {𝓒}
        ))),
        {𝐃 = {𝐗}}
      },
      𝐘 = {
        #(𝐘: %Element($../Objects(
          u = {u},
          𝓒 = {𝓒}
        ))),
        {𝐙 = {
          #(𝐙: %Element($../Objects(
            u = {u},
            𝓒 = {𝓒}
          ))),
          {f = {
            #(f: %Element($../Morphisms(
              u = {u},
              𝓒 = {𝓒},
              𝐗 = {𝐘},
              𝐘 = {𝐙}
            ))),
            {d = {f}}
          }}
        }}
      }
  )]
}
