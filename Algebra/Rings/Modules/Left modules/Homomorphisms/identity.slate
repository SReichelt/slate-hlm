%../../../../../../../logics/hlm%

/**
 * @references * https://leanprover-community.github.io/mathlib_docs/algebra/module.html#linear_map.id
 */
$identity(
  𝐑: %Element(
    $../../../Rings,
    auto = %true
  ),
  𝐌: %Element(
    $../"Left modules"(𝐑 = {𝐑}),
    auto = %true
  )
): %ExplicitOperator {
  display = [$SubSup(
    body = 'id',
    sub = 𝐌
  )],
  definition = [$../../../../../Essentials/Functions/identity(X = {$../Carrier(
    𝐑 = {𝐑},
    𝐌 = {𝐌}
  )})]
}
