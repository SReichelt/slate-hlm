%../../../../../../../logics/library%

$Submodules: %Section {
  logic = 'hlm',
  items = [
    %item($"submodule set"),
    %item($"subset module"),
    %item($submodule),
    %item($Span),
    %item($"Span of element")
  ]
}
