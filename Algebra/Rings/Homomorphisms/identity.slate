%../../../../../logics/hlm%

$identity(𝐑: %Element(
  $../Rings,
  auto = %true
)): %ExplicitOperator {
  display = [$SubSup(
    body = 'id',
    sub = 𝐑
  )],
  definition = [$../../../Essentials/Functions/identity(X = {$../Carrier(𝐑 = {𝐑})})]
}
