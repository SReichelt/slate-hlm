%../../../../../logics/library%

$Polynomials: %Section {
  logic = 'hlm',
  items = [
    %item($Polynomials),
    %item($normalized),
    %item($monomial),
    %item($"monomial with exponent one"),
    %item($sum),
    %item($"additive inverse"),
    %item($difference),
    %item($"product with left scalar"),
    %item($"product with right scalar"),
    %item($product),
    %item($"polynomial ring"),
    %item($value),
    %item($dimension),
    %item($degree)
  ]
}
