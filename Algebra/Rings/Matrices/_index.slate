%../../../../../logics/library%

$Matrices: %Section {
  logic = 'hlm',
  items = [
    %item($Matrices),
    %item($Vectors),
    %item($matrix, title = 'Matrix notation'),
    %item($sum),
    %item($zero),
    %item($"additive inverse"),
    %item($difference),
    %item($product),
    %item($"product with left scalar"),
    %item($"product with right scalar"),
    %item($identity),
    %item($"matrix ring"),
    %subsection($Examples, 'Examples')
  ]
}
