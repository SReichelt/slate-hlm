%../../../../../logics/hlm%

$identity(𝐌: %Element(
  $../Magmas,
  auto = %true
)): %ExplicitOperator {
  notation = $SubSup(body = 'id', sub = 𝐌),
  definition = [$../../../Essentials/Functions/identity(X = $../Carrier(𝐌 = 𝐌))]
}
