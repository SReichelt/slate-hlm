%../../../../../logics/library%

$Examples: %Section {
  logic = 'hlm',
  items = [
    %item($"rational numbers"),
    %item($"real numbers"),
    %item($"complex numbers"),
    %item($"prime field")
  ]
}
