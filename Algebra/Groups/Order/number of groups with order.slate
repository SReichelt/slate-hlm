%../../../../../logics/hlm%

/**
 * @references * http://oeis.org/A000001
 */
$"number of groups with order"(n: %Element($../../../Essentials/Numbers/Natural/"Natural numbers")): %ExplicitOperator {
  display = [$Function(
    function = 'gnu',
    arguments = [n]
  )],
  definition = [$../../../Essentials/Numbers/Cardinal/"Cardinal numbers".cardinality(S = {$"Groups with order"(κ = {n})})]
}
