%../../../../../logics/library%

$Order: %Section {
  logic = 'hlm',
  items = [
    %item($"group order"),
    %item($"Groups with order"),
    %item($"number of groups with order"),
    %item($"Number of small groups")
  ]
}
