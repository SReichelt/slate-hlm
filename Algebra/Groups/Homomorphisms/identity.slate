%../../../../../logics/hlm%

/**
 * @references * https://leanprover-community.github.io/mathlib_docs/algebra/group/hom.html#monoid_hom.id
 */
$identity(𝐆: %Element(
  $../Groups,
  auto = %true
)): %ExplicitOperator {
  notation = $SubSup(body = 'id', sub = 𝐆),
  definition = [$../../../Essentials/Functions/identity(X = $../Carrier(𝐆 = 𝐆))]
}
