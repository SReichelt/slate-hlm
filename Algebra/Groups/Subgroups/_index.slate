%../../../../../logics/library%

$Subgroups: %Section {
  logic = 'hlm',
  items = [
    %item($"subgroup set"),
    %item($"subset group"),
    %item($subgroup),
    %item($"normal subgroup set"),
    %item($"normal subgroup"),
    %item($Span),
    %item($"Span of element")
  ]
}
