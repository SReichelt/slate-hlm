%../../../../../logics/library%

$Examples: %Section {
  logic = 'hlm',
  items = [
    %item($integers),
    %item($"cyclic group"),
    %item($"symmetric group"),
    %item($"finite symmetric group")
  ]
}
