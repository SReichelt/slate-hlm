%../../../../logics/hlm%

$identity(𝐆: %Element(
  $Groups,
  auto = %true
)): %ExplicitOperator {
  display = [$SubSup(
    body = 'id',
    sub = 𝐆
  )],
  definition = [$../../Essentials/Functions/identity(X = {$Carrier(𝐆 = {𝐆})})]
}
