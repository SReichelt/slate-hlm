%../../../../../logics/hlm%

/**
 * @remarks This follows immediately from the HLM-specific definition of equality of pointed sets.
 */
$"Equality criterion"(𝐒,𝐓: %Element($../"Pointed sets")): %EquivalenceTheorem {
  conditions = [
    %not($../../../Essentials/Sets/empty(S = $Isomorphisms(
      𝐒 = 𝐒,
      𝐓 = 𝐓
    ))),
    %equals(𝐒, 𝐓)
  ]
}
