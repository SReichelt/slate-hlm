%../../../../logics/hlm%

$"base point"(𝐒: %Element($"Pointed sets")): %ExplicitOperator {
  notation = $Function(
    function = 'base',
    arguments = [𝐒]
  ),
  definition = [%structuralCases(
    𝐒, $"Pointed sets",
    [{
      constructor = $"Pointed sets"."pointed set",
      parameters = #(
        S: %Set,
        s: %Element(S)
      ),
      value = s
    }]
  )]
}
