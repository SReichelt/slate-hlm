%../../../../../logics/hlm%

$identity(𝐑: %Element(
  $../Semirings,
  auto = %true
)): %ExplicitOperator {
  display = [$SubSup(
    body = 'id',
    sub = 𝐑
  )],
  definition = [$../../../Essentials/Functions/identity(X = {$../Carrier(𝐑 = {𝐑})})]
}
