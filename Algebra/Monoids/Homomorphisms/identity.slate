%../../../../../logics/hlm%

$identity(𝐌: %Element(
  $../Monoids,
  auto = %true
)): %ExplicitOperator {
  display = [$SubSup(
    body = 'id',
    sub = 𝐌
  )],
  definition = [$../../../Essentials/Functions/identity(X = {$../Carrier(𝐌 = {𝐌})})]
}
