%../../../../../logics/library%

$Isomorphisms: %Section {
  logic = 'hlm',
  items = [
    %item($"element condition"),
    %item($"function condition"),
    %item($"inner operation condition"),
    %item($"left operation condition"),
    %item($"right operation condition"),
    %item($"relation condition"),
    %item($"morphism condition")
  ]
}
