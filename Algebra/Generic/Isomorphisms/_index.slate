%../../../../../logics/library%

$Isomorphisms: %Section {
  logic = 'hlm',
  items = [
    %item($"element transport"),
    %item($"function transport"),
    %item($"inner operation transport"),
    %item($"left operation transport"),
    %item($"right operation transport"),
    %item($"relation transport"),
    %item($"morphism transport")
  ]
}
