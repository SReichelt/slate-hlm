%../../../../logics/library%

$"Untyped lambda calculus": %Section {
  logic = 'hlm',
  items = [
    %item($Terms),
    %item($substitution),
    %item($rewritable),
    %item($"beta reduction", title = 'β reduction'),
    %item($"eta conversion", title = 'η conversion'),
    %subsection($Combinators, 'Combinators')
  ]
}
