%../../../../../logics/hlm%

$quotient(
  a: %Element($"Complex numbers"),
  b: %Element($Subsets/"Nonzero numbers")
): %ImplicitOperator {
  notation = $Fraction(
    numerator = a,
    denominator = b
  ),
  parameter = #(x: %Element($"Complex numbers")),
  definition = [%equals(
    a,
    $product(
      a = {x},
      b = {b}
    )
  )]
}
