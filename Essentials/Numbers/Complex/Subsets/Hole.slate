%../../../../../../logics/hlm%

[
  $~"Complex numbers" = $../"Complex numbers"
]

$Hole(a: %Element($~"Complex numbers")): %SetOperator {
  notation = $Segment(set = 'ℂ', symbol = '≠', limit = a),
  definition = [%subset(
    #(b: %Element($~"Complex numbers")),
    %not(%equals(b, a))
  )]
}
