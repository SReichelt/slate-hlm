%../../../../../../logics/hlm%

[
  $~Integers = $../Integers
]

$"Segment (less)"(a: %Element($~Integers)): %SetOperator {
  notation = $Segment(set = 'ℤ', symbol = '<', limit = a),
  definition = [%subset(
    #(b: %Element($~Integers)),
    $../less(a = b, b = a)
  )]
}
