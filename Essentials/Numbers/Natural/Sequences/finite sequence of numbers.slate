%../../../../../../logics/hlm%

$"finite sequence of numbers"(items: %Nat[]): %MacroOperator {
  display = [$Tuple(
    items = items,
    style = '()'
  )],
  variables = #(length: %Nat),
  references = {
    "Fixed-length sequences" = $../../../Sequences/"Fixed-length sequences"(
      S = {$../"Natural numbers"},
      α = {$../number(value = length)}
    ),
    "finite sequence" = $../../../Sequences/"finite sequence"
  }
}
