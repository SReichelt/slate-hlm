%../../../../../../logics/library%

$Sequences: %Section {
  logic = 'hlm',
  items = [
    %item($"indexed element"),
    %item($"bounded cardinality"),
    %item(
      $"tuple of numbers",
      title = 'Tuple'
    ),
    %item($"sequence from subset"),
    %item($"infinite sequence from subset")
  ]
}
