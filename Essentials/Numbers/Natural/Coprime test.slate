%../../../../../logics/hlm%

[
  $~"Segment (greater)" = $Subsets/"Segment (greater)"
]

$"Coprime test"(m,n: %Element($~"Segment (greater)"(n = $number(value = 1)))): %StandardTheorem {
  claim = %not($coprime(
    m = m,
    n = $product(m = m, n = n)
  ))
}
