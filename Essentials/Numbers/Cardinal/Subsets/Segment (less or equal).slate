%../../../../../../logics/hlm%

$"Segment (less or equal)"(κ: %Element($../"Cardinal numbers")): %SetOperator {
  notation = [$Segment(
    set = '𝓒𝓻𝓭',
    symbol = '≤',
    limit = κ
  )],
  definition = [%subset(
    #(μ: %Element($../"Cardinal numbers")),
    $../"less or equal"(
      κ = {μ},
      μ = {κ}
    )
  )]
}
