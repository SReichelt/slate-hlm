%../../../../../../logics/library%

$Subsets: %Section {
  logic = 'hlm',
  items = [
    %item($"Segment (less)"),
    %item($"Segment (less or equal)"),
    %item($"Segment (greater or equal)"),
    %item($"Segment (greater)")
  ]
}
