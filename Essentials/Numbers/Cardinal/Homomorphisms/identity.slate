%../../../../../../logics/hlm%

$identity(𝐱: %Element(
  $../"Cardinal numbers",
  auto = %true
)): %ExplicitOperator {
  display = [$SubSup(
    body = 'id',
    sub = 𝐱
  )],
  definition = [$../../../Functions/identity(X = {$../Carrier(𝐱 = {𝐱})})]
}
