%../../../../../../logics/hlm%

$identity(α: %Element($../"Ordinal numbers")): %ExplicitOperator {
  notation = [$SubSup(
    body = 'id',
    sub = α
  )],
  definition = [$../../../Functions/identity(X = {$../Carrier(α = {α})})]
}
