%../../../../../../logics/hlm%

[
  $~"Ordinal numbers" = $../"Ordinal numbers"
]

$"Segment (greater)"(α: %Element($~"Ordinal numbers")): %SetOperator {
  notation = $Segment(set = '𝓞𝓻𝓭', symbol = '>', limit = α),
  definition = [%subset(
    #(β: %Element($~"Ordinal numbers")),
    $../less(β = β, α = α)
  )]
}
