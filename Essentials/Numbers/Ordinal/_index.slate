%../../../../../logics/library%

$Ordinal: %Section {
  logic = 'hlm',
  items = [
    %item($"Ordinal numbers"),
    %item($Carrier),
    %item($before),
    %subsection(
      $Homomorphisms,
      'Homomorphisms'
    ),
    %subsection(
      $Subsets,
      'Subsets'
    ),
    %item($"less or equal"),
    %item($less),
    %item($"Less or equal is a well-order"),
    %item($sum),
    %item($product)
  ]
}
