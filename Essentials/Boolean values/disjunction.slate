%../../../../logics/hlm%

$disjunction(a,b: %Element($"Boolean values")): %ExplicitOperator {
  notation = $Operator(
    symbol = '∨',
    operands = [a, b],
    space = ' '
  ),
  definition = [$"Boolean values".value(p = %or(
    $"is true"(b = a),
    $"is true"(b = b)
  ))]
}
