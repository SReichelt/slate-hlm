%../../../../logics/hlm%

$"finite sequence"(
  S: %Set,
  items: %Element(S)[]
): %MacroOperator {
  display = [$SubSup(
    body = $Tuple(
      items = items,
      style = '()'
    ),
    sub = S
  )],
  variables = #(length: %Nat),
  references = {"Fixed-length sequences" = $"Fixed-length sequences"(
    S = {S},
    α = {$../Numbers/Natural/number(value = length)}
  )}
}
