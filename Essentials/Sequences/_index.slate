%../../../../logics/library%

$Sequences: %Section {
  logic = 'hlm',
  items = [
    %item($"Finite sequences"),
    %item(
      $"finite sequence",
      title = 'Tuple'
    ),
    %item($"Infinite sequences"),
    %item($"starts with")
  ]
}
