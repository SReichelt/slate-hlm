%../../../../logics/hlm%

$finite(
  S: %Set(auto = %true),
  𝒂: %Element($"Ordinal-indexed sequences"(S = {S}))
): %Predicate {
  display = [$Property(
    operand = 𝒂,
    property = 'finite'
  )],
  definition = [$../Numbers/Ordinal/finite(α = {$"ordinal-indexed sequence length"(
    S = {S},
    𝒂 = {𝒂}
  )})]
}
