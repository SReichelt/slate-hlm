%../../../../../../logics/library%

$"Generalized operations": %Section {
  logic = 'hlm',
  items = [
    %item($"Generalized operations"),
    %item($"neutral element"),
    %item($operation),
    %item($"indexed by natural number"),
    %item($"Generalized operation indexed by natural number is distributive"),
    %item($"indexed by integer"),
    %item($"indexed by finite set"),
    %item($"indexed by set with finite support")
  ]
}
